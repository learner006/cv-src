const editorManager = {
}

const imageProvider = {
}

const stateFactory = () => {
  return {
    imageProvider,
    editorManager: editorManager,
    imageProvider: imageProvider
  }
}

export default stateFactory;